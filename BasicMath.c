#include <stdio.h>
#include "myMath.h"
float add (double x, double y){
    return (x+y);
}
float sub (double x, double y){
    return (x-y);
}
float mul (double x, double y){
    return (x*y);
}
float divi (double x, double y){
    return (x/y);
}